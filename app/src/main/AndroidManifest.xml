<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.Fadhilah_Ramadhan.TokoKomputer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".fragment.checkout" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Profil_data_edit" />
        <activity android:name=".Profil_data_alamat" />
        <activity android:name=".Pembayaran_keranjang" />
        <activity android:name=".Pembayaran_langsung" />
        <activity android:name=".riwayat_transaksi" />
        <activity android:name=".riwayat_transaksi_detail" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".admin.tambah_barang" />
        <activity android:name=".admin.edit_barang" />
        <activity android:name=".admin.KonfirmasiBarang" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>